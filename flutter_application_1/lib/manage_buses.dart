import 'package:flutter/material.dart';

class ManageBuses extends StatelessWidget {
  const ManageBuses({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}