import 'package:flutter/material.dart';

class ManageCosts extends StatelessWidget {
  const ManageCosts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}