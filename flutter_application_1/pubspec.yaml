name: flutter_application_1
description: "A new Flutter project"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2
  flutter_screenutil: ^5.9.3
  get: ^4.7.3
  gap: ^3.0.1
  pdf: ^3.10.7
  printing: ^5.12.0

  # Firebase (match FlutterFire CLI output)
  firebase_core: ^3.15.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true